# Task 09 — CI and Release

Goal: prepare repeatable builds.

Steps:

1. Add GitHub Actions for macOS build.
2. Add Windows build after macOS works.
3. Upload VST3 artifacts.
4. Add version stamping.
5. Add preset model format migration plan.
6. Add release checklist.

Acceptance:

- CI can build from clean checkout.
- dependency versions are pinned.
- artifacts are produced without local-only paths.
